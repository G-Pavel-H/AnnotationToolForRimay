{
  "name": "rimay-annotation-tool",
  "version": "1.0.0",
  "private": true,
  "description": "Local web app for annotating requirements into Rimay. Run `npm run setup` then `npm start`.",
  "scripts": {
    "setup": "node setup.js",
    "start": "node start.js"
  }
}
